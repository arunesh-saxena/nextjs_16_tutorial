import Card from '@/app/_components/card';
import React from 'react';

const RevenuePage = () => {
    return (
        <Card title={'Revenue'} description={'This is the revenue page.'}>
            <p>Revenue Content</p>
        </Card>
    );
};

export default RevenuePage;
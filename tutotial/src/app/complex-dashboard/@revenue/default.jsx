import React from 'react';
import Card from '@/app/_components/card';

const RevenueDashboard = () => {
    return (
        <Card title={'Revenue Dashboard'}
            description={'This is the revenue dashboard.'}>
            default content
        </Card>
    );
};

export default RevenueDashboard;
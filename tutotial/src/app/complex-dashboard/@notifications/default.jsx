import React from 'react';
import Card from '@/app/_components/card';

const Notifications = () => {
    return (
        <Card title={'Notifications'} description={'This is the notifications component.'}>

            <p>Content.</p>
        </Card>
    );
};

export default Notifications;